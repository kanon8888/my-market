import React, { use } from 'react';

const Dresst = ({ singleDress }) => {
    // const data = use(dressPromish);
    // console.log(data)
    console.log(singleDress)
    return (
        <div>
            <h1>single Dress</h1>
        </div>
    );
};

export default Dresst;